# Development HPA patch
# Disable autoscaling in dev environment
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: user-service-hpa
spec:
  minReplicas: 1
  maxReplicas: 2  # Max 2 pods in dev
