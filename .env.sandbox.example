# Sandbox Environment Configuration (QA/Testing)
# Synthetic live-like data for testing

APP_ENV=sandbox

# Server Configuration
SERVER_PORT=8080
SERVER_HOST=0.0.0.0
GRPC_PORT=9090

# Database Configuration  
DB_HOST=sandbox-db.pandora-exchange.internal
DB_PORT=5432
DB_USER=pandora_sandbox
DB_PASSWORD=<VAULT:secret/sandbox/db/password>
DB_NAME=pandora_sandbox
DB_SSLMODE=require

# JWT Configuration
JWT_SECRET=<VAULT:secret/sandbox/jwt/secret>
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=168h

# Redis Configuration
REDIS_HOST=sandbox-redis.pandora-exchange.internal
REDIS_PORT=6379
REDIS_PASSWORD=<VAULT:secret/sandbox/redis/password>
REDIS_DB=0
